export function Footer() {
  return (
    <footer
      style={{
        borderTop: "1px solid var(--border)",
        padding: "1.5rem 1rem",
        maxWidth: "48rem",
        margin: "0 auto",
        fontSize: "0.95rem",
        color: "#444",
      }}
    >
      <p>복지 서비스 길잡이 — 초기 scaffold</p>
    </footer>
  );
}
