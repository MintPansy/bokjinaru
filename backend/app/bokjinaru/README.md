# paperdot (Spring Boot)

가벼운 API scaffold. `GET /api/health` 만 제공합니다.

## 실행

IDE에서 `PaperdotApplication` 실행, 또는 (Gradle 설치 시):

```bash
gradle bootRun
```

Java **17** 권장. Wrapper는 필요 시 `gradle wrapper`로 생성하세요.
